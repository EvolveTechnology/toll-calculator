export const oneMinute = 60 * 1000;
export const halfHour = 30 * oneMinute;
export const oneHour = 60 * oneMinute;
export const oneDay = 24 * oneHour;

// For this problem, there are no discounts
// or a need to introduce cents
// so we can just use continuous numbers to
// describe money
export const MAX_FEE = 60;
